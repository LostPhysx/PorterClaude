# PorterClaude release: multi-arch app image to ghcr.io on tags. OWNER: O2.
# Spec: docs/design/orchestration.md §8. Recipe images are NOT released — they are built on
# the target host by the app itself (see docker/README.md).
name: release

on:
  push:
    tags: ['v*']
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  image:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      # TODO(O2):
      #   docker/login-action@v3   registry: ghcr.io, username: ${{ github.actor }},
      #                            password: ${{ secrets.GITHUB_TOKEN }}
      #   docker/metadata-action@v5  images: ghcr.io/${{ github.repository }}
      #     tags: type=semver,pattern={{version}} | {{major}}.{{minor}} | type=raw,value=latest
      #   docker/build-push-action@v6  context: ., file: docker/Dockerfile, push: true,
      #     platforms: linux/amd64,linux/arm64, provenance: false,
      #     cache-from: type=gha, cache-to: type=gha,mode=max
      - run: echo "TODO(O2)"
